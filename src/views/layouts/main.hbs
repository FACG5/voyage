<!DOCTYPE html>
<html lang="en">
<head>
    {{>head}}
</head>
    <body>
        {{>header}}

        <main>
            {{{body}}}
        </main>

        {{>footer}}
    </body>
    {{#if vald}}
    <script src="/js/{{vald}}.js"></script>
    {{/if}}
    {{#if dom}}
    <script src="/js/{{dom}}.js"></script>
    {{/if}}
    {{#if script}}
    <script src="/js/{{script}}.js"></script>
    {{/if}}
</html>
