<!DOCTYPE html>
<html lang="en">
<head>
    {{>head}}
</head>
    <body>
        {{>header}}

        <main>
            {{{body}}}
        </main>

        {{>footer}}
    </body>

    {{#if comment}}
        <script src="/js/{{comment}}.js"></script>
    {{/if}}

    {{#if vald}}
    <script src="/js/{{vald}}.js"></script>
    {{/if}}

    {{#if dom}}
        <script src="/js/{{dom}}.js"></script>
    {{/if}}

    {{#if isPerson }}
        <script src="/js/{{personAddReview}}.js"></script>
    {{/if}}

    {{#if script}}
    <script src="/js/{{script}}.js"></script>
    {{/if}}

</html>
