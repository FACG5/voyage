<!DOCTYPE html>
<html lang="en">
<head>
    {{>head}}
</head>
    <body>
        {{>header}}

        <main>
            {{{body}}}
        </main>

        {{>footer}}
    </body>
    {{#if dom}}
    <script src="./js/{{dom}}.js"></script>
    {{/if}}
</html>
