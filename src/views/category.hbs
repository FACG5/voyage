<div class="cat-result">
  {{#each response}}
  <div class="cat-result-pic" >
    <img class="cat-photo" id="{{name}}" src="{{img}}">
  </div>
    <h3 class="cat-name">{{name}}</h3>
    <div class="avg-evaluation">
        <h3>Avg Evaluation : {{avg}}/5 </h3>
    </div>
  {{/each}}
  <div class="rgba"><div>
</div>
